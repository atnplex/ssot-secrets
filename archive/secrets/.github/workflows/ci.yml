name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  governance:
    uses: atnplex/legacy-actions/.github/workflows/reusable-governance.yml@main
